from django.db import models
from django.contrib.auth.models import User

# Como estamos utilizando el modelo User de Django, no necesitamos crear modelos adicionales
# En una aplicación más compleja, podríamos definir un modelo Perfil para extender User